interface:
  display_name: "Pizza App"
  short_description: "Repo-specific patterns and workflows for pizza-app"
  default_prompt: "Use the pizza-app repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true